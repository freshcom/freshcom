dist: xenial
services:
  - postgresql
addons:
  postgresql: "10"
language: elixir
elixir: '1.7'
otp_release: '21.0'
before_script:
  - mix freshcom.setup
script:
  - cd services/fc_identity; mix test
  - cd ../../; mix test
env:
  global:
    - DB_USERNAME="postgres"
    - MIX_ENV="test"